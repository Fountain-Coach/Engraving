rule: RULE.Barline.style_and_break_policy
cases:
  - name: thickness_and_padding
    expectations:
      - { path: "/thickness", op: ">=", value: 0 }
      - { path: "/paddingSP", op: ">=", value: 0 }
