rule: RULE.Lyrics.hyphen_melisma_spacing_interaction
cases:
  - name: hyphen_gap_minimum
    expectations:
      - { path: "/lyricOffsets/count", op: ">=", value: 1 }
      - { path: "/baselineYOffsetSP", op: ">=", value: 0.0 }
